# Set a unique local database password before starting Docker Compose.
POSTGRES_PASSWORD=
WEB_PORT=8080
# Optional exact browser origin. Defaults to http://localhost:<WEB_PORT>.
# APP_ORIGIN=http://localhost:8080
# Fixed session lifetime: 60-86400 seconds. HTTPS origins use Secure cookies.
SESSION_TTL_SECONDS=28800
# Aircraft coverage: sao_paulo or worldwide.
AVIATION_SCOPE=worldwide
# Shared snapshot cache. At 120 seconds, worldwide coverage consumes 2,880 of
# a standard authenticated OpenSky account's 4,000 daily state credits.
AVIATION_CACHE_TTL_SECONDS=120
# OAuth is required for worldwide coverage. São Paulo may run anonymously only
# with AVIATION_CACHE_TTL_SECONDS=216 or higher.
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
# Optional provider overrides for development or compatible test services.
# AVIATION_API_BASE_URL=https://opensky-network.org/api
# OPENSKY_TOKEN_URL=https://auth.opensky-network.org/auth/realms/opensky-network/protocol/openid-connect/token
